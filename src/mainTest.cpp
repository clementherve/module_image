#include "Image.h"
 
int main() {
   Image monImage(4, 4);

   // Pixel pix(5, 6, 7);
   // monImage.dessinerRectangle(0, 0, 2, 2, pix);
   // monImage.afficher();
   monImage.testRegression();
   return 0;
}